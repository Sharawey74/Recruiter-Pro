# Recruiter-Pro - development dependencies
#
# Tests, linting and formatting. Not needed to run or deploy the app.
#
#   pip install -r requirements.txt -r requirements-ml.txt -r requirements-dev.txt
#
# The ML extras are included because a large part of the suite exercises the
# training pipeline.

-r requirements.txt
-r requirements-ml.txt

# --- Testing ---
pytest==8.4.1
pytest-cov==7.0.0
pytest-mock==3.15.1
pytest-timeout>=2.2,<3.0
httpx>=0.25,<1.0             # required by fastapi.testclient

# --- Linting and formatting ---
black>=24.0.0
ruff>=0.4,<1.0               # replaces flake8 + isort; one config, much faster
mypy>=1.8.0

# Note: flake8 was dropped in favour of ruff. `flake8 src/` reported 1,565
# issues and was never wired into anything; ruff covers the same ground with a
# single config block in pyproject.toml. See TASKS.md 6.3.
