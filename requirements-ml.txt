# Recruiter-Pro - model training dependencies
#
# Needed only to run `python -m src.ml_engine.train`. The API does not import
# any of these; it loads a pre-built .joblib and needs scikit-learn + joblib
# from requirements.txt alone.
#
# Kept separate so the deployed image does not carry a plotting stack it never
# uses - the target free tier has 512 MB of RAM.
#
#   pip install -r requirements.txt -r requirements-ml.txt

-r requirements.txt

xgboost>=2.0,<3.0            # MISSING BEFORE - 2 imports
imbalanced-learn>=0.11,<1.0  # MISSING BEFORE - 2 imports, imported as `imblearn`
matplotlib>=3.7,<4.0         # MISSING BEFORE - 1 import, learning curves
seaborn>=0.13,<1.0           # MISSING BEFORE - 1 import
